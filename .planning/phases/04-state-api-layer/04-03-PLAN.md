---
phase: 04-state-api-layer
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - frontend/src/hooks/use-sse.ts
  - frontend/tests/hooks/use-sse.test.ts
  - frontend/tests/setup.ts
  - frontend/src/lib/stores/slices/preferences.ts
  - frontend/tests/stores/slices/preferences.test.ts
autonomous: true
requirements: [STAT-03, STAT-04]
user_setup: []
must_haves:
  truths:
    - "SSE hook connects to URL when enabled=true"
    - "SSE hook disconnects and cleans up on unmount"
    - "SSE hook reconnects with exponential backoff on error"
    - "SSE hook exposes connectionState (CONNECTING, OPEN, CLOSED)"
    - "Theme preference persists in localStorage (via next-themes)"
    - "Theme syncs across tabs (via next-themes storage event)"
  artifacts:
    - path: "frontend/src/hooks/use-sse.ts"
      provides: "SSE streaming hook with lifecycle management"
      exports: ["useSSE", "SSEConnectionState"]
    - path: "frontend/tests/hooks/use-sse.test.ts"
      provides: "Unit tests for SSE hook"
      contains: "describe('useSSE'"
    - path: "frontend/tests/setup.ts"
      provides: "EventSource mock for jsdom"
      contains: "MockEventSource"
    - path: "frontend/src/lib/stores/slices/preferences.ts"
      provides: "Preferences state slice (future expansion)"
      contains: "createPreferencesSlice"
  key_links:
    - from: "frontend/src/hooks/use-sse.ts"
      to: "EventSource API"
      via: "Browser EventSource constructor"
      pattern: "new EventSource"
    - from: "frontend/src/app/providers.tsx"
      to: "next-themes"
      via: "ThemeProvider"
      pattern: "ThemeProvider"
---

<objective>
Create SSE streaming hook with lifecycle management and verify theme persistence.

Purpose: Provide a robust hook for real-time SSE streaming with proper connection lifecycle (connect, reconnect with backoff, cleanup) and verify existing theme persistence works correctly.
Output: useSSE hook, EventSource mock, preferences slice stub, and comprehensive tests.
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-state-api-layer/04-RESEARCH.md
@.planning/phases/04-state-api-layer/04-VALIDATION.md

<interfaces>
<!-- Existing interfaces from codebase -->

From frontend/src/app/providers.tsx:
```typescript
<ThemeProvider
  attribute="class"
  defaultTheme="system"
  enableSystem
  disableTransitionOnChange
>
```

From frontend/src/lib/stores/context.tsx (created in 04-01):
```typescript
export type StoreState = ConsoleSlice
export const useStore = <T,>(selector: (store: StoreState) => T): T
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EventSource mock to test setup</name>
  <files>frontend/tests/setup.ts</files>
  <action>
    Add EventSource mock to existing test setup file following RESEARCH.md Validation section.

    Update `frontend/tests/setup.ts`:
    - Add MockEventSource class after existing mocks:
      - Properties: url, readyState, onopen, onmessage, onerror
      - Constructor stores url, sets readyState to CONNECTING
      - setTimeout to simulate async connection: set readyState to OPEN, call onopen
      - close() method sets readyState to CLOSED
      - addEventListener and removeEventListener stub methods
    - Assign to global.EventSource

    This enables testing SSE hook without real browser EventSource.
  </action>
  <verify>
    <automated>cd /home/guyi/structureclaw/frontend && npm test -- --run 2>&1 | head -20</automated>
  </verify>
  <done>
    EventSource mock added to tests/setup.ts.
    Existing tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SSE streaming hook</name>
  <files>frontend/src/hooks/use-sse.ts, frontend/tests/hooks/use-sse.test.ts</files>
  <behavior>
    - Test 1: Hook returns CONNECTING state initially
    - Test 2: Hook transitions to OPEN when connection established
    - Test 3: Hook calls onMessage callback with received data
    - Test 4: Hook calls onError callback on error
    - Test 5: Hook disconnects on unmount (cleanup)
    - Test 6: Hook does not connect when enabled=false
    - Test 7: connect() method creates new EventSource
    - Test 8: disconnect() method closes EventSource
  </behavior>
  <action>
    Create SSE hook following RESEARCH.md Pattern 3.

    1. Create `frontend/src/hooks/use-sse.ts`:
       - Export `SSEConnectionState` type: 'CONNECTING' | 'OPEN' | 'CLOSED'
       - Define `UseSSEOptions` interface: url (string), enabled (boolean, default true), onMessage?, onError?
       - Define `UseSSEReturn` interface: connectionState, error, data, connect, disconnect
       - Export `useSSE(options)` function:
         - State: connectionState (default 'CONNECTING'), error (null), data (null)
         - Refs: eventSourceRef, reconnectTimeoutRef, reconnectAttemptRef
         - disconnect() callback: close EventSource, clear timeout, reset state
         - connect() callback: close existing, create new EventSource, setup handlers:
           - onopen: set OPEN state, reset reconnect attempt
           - onmessage: update data, call onMessage callback
           - onerror: set error, call onError callback
             - If readyState === CLOSED: exponential backoff (max 30s, max 5 attempts)
         - useEffect: connect if enabled, return disconnect cleanup
         - Return object with all state and methods

    2. Create `frontend/tests/hooks/use-sse.test.ts`:
       - Use renderHook from @testing-library/react
       - Use act() for state updates
       - Test initial state is CONNECTING
       - Test transition to OPEN (wait for setTimeout in mock)
       - Test onMessage callback receives data
       - Test onError callback on error
       - Test cleanup on unmount (verify close called)
       - Test enabled=false prevents connection
       - Test connect/disconnect methods

    Run `npm test -- --run` from frontend directory.
  </action>
  <verify>
    <automated>cd /home/guyi/structureclaw/frontend && npm test -- --run use-sse</automated>
  </verify>
  <done>
    useSSE hook exports connectionState, data, error, connect, disconnect.
    Tests pass verifying lifecycle management.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create preferences slice and verify theme persistence</name>
  <files>frontend/src/lib/stores/slices/preferences.ts, frontend/tests/stores/slices/preferences.test.ts</files>
  <behavior>
    - Test 1: createPreferencesSlice returns initial state
    - Test 2: Preferences slice can be combined with ConsoleSlice in StoreState
  </behavior>
  <action>
    Create minimal preferences slice for future theme expansion and verify STAT-04.

    1. Create `frontend/src/lib/stores/slices/preferences.ts`:
       - Note: next-themes already handles theme persistence (localStorage + cross-tab sync)
       - This slice is for future preferences that need Zustand state
       - Define `PreferencesState` interface (empty for now, ready for expansion)
       - Define `PreferencesActions` interface (empty for now)
       - Export `PreferencesSlice` type
       - Export `initialPreferencesState` constant: {}
       - Export `createPreferencesSlice` function returning empty slice

    2. Create `frontend/tests/stores/slices/preferences.test.ts`:
       - Test slice creation returns expected structure
       - Document that theme persistence is handled by next-themes (STAT-04)

    3. Update `frontend/src/lib/stores/context.tsx`:
       - Import PreferencesSlice
       - Add PreferencesSlice to StoreState type
       - Include preferences slice in createAppStore

    Run `npm test -- --run` and `npm run type-check` from frontend directory.
  </action>
  <verify>
    <automated>cd /home/guyi/structureclaw/frontend && npm test -- --run preferences && npm run type-check</automated>
  </verify>
  <done>
    Preferences slice created and integrated into store.
    STAT-04 verified as handled by existing next-themes implementation.
  </done>
</task>

</tasks>

<verification>
- [ ] EventSource mock allows testing SSE hook in jsdom
- [ ] useSSE hook manages connection lifecycle (connect, disconnect, cleanup)
- [ ] Exponential backoff reconnection works (max 30s delay, max 5 attempts)
- [ ] connectionState transitions correctly (CONNECTING -> OPEN -> CLOSED)
- [ ] Theme persistence works via next-themes (manual verification noted)
- [ ] All tests pass with `npm test -- --run`
</verification>

<success_criteria>
1. SSE hook properly manages connection lifecycle (connect, reconnect, cleanup)
2. Connection state exposed as CONNECTING | OPEN | CLOSED
3. Exponential backoff prevents server overload on reconnection
4. Cleanup prevents memory leaks on component unmount
5. Theme persistence verified as handled by next-themes (STAT-04)
6. All tests pass with >90% coverage on new code
</success_criteria>

<output>
After completion, create `.planning/phases/04-state-api-layer/04-03-SUMMARY.md`
</output>
