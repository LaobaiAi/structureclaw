---
phase: 04-state-api-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api/errors.ts
  - frontend/src/lib/api/client.ts
  - frontend/src/lib/api/index.ts
  - frontend/tests/api/errors.test.ts
  - frontend/tests/api/client.test.ts
autonomous: true
requirements: [STAT-02]
user_setup: []
must_haves:
  truths:
    - "API client returns typed responses for successful requests"
    - "API client throws ApiError with status and data on HTTP errors"
    - "API client throws NetworkError on fetch failures"
    - "Convenience methods (get, post, put, delete) work correctly"
  artifacts:
    - path: "frontend/src/lib/api/errors.ts"
      provides: "Typed error classes for API failures"
      exports: ["ApiError", "NetworkError"]
    - path: "frontend/src/lib/api/client.ts"
      provides: "Centralized fetch wrapper with error handling"
      exports: ["apiClient", "api"]
    - path: "frontend/tests/api/client.test.ts"
      provides: "Unit tests for API client"
      contains: "describe('api client'"
  key_links:
    - from: "frontend/src/lib/api/client.ts"
      to: "process.env.NEXT_PUBLIC_API_URL"
      via: "API_BASE constant"
      pattern: "NEXT_PUBLIC_API_URL"
---

<objective>
Create centralized API client with typed responses and consistent error handling.

Purpose: Centralize all API fetch logic with proper TypeScript types, error handling, and convenience methods for HTTP verbs.
Output: Error classes, API client with get/post/put/delete methods, and comprehensive tests.
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-state-api-layer/04-RESEARCH.md
@.planning/phases/04-state-api-layer/04-VALIDATION.md

<interfaces>
<!-- Existing interfaces from codebase -->

From backend API (per ARCHITECTURE.md):
```typescript
// Backend response format:
{
  success: boolean;
  error_code?: string | null;
  message?: string;
  data?: Record<string, unknown>;
  meta?: Record<string, unknown>;
}
```

Environment variable:
```typescript
process.env.NEXT_PUBLIC_API_URL // defaults to http://localhost:8000
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create error classes</name>
  <files>frontend/src/lib/api/errors.ts, frontend/tests/api/errors.test.ts</files>
  <behavior>
    - Test 1: ApiError has status, statusText, and optional data properties
    - Test 2: ApiError message includes status and statusText
    - Test 3: NetworkError has default message
    - Test 4: NetworkError accepts custom message
    - Test 5: Both errors have correct name property
  </behavior>
  <action>
    Create typed error classes following RESEARCH.md Pattern 2.

    1. Create `frontend/src/lib/api/errors.ts`:
       - Export `ApiError` class extending Error:
         - Constructor takes status (number), statusText (string), optional data (unknown)
         - Sets message to `API Error: ${status} ${statusText}`
         - Sets name to 'ApiError'
         - Stores status, statusText, data as public properties
       - Export `NetworkError` class extending Error:
         - Constructor takes optional message (default: 'Network request failed')
         - Sets name to 'NetworkError'

    2. Create `frontend/tests/api/errors.test.ts`:
       - Test ApiError construction with all properties
       - Test ApiError without data
       - Test NetworkError with default message
       - Test NetworkError with custom message
       - Verify error names are set correctly

    Run `npm test -- --run` from frontend directory.
  </action>
  <verify>
    <automated>cd /home/guyi/structureclaw/frontend && npm test -- --run errors</automated>
  </verify>
  <done>
    ApiError and NetworkError classes exported.
    Tests pass verifying error properties and messages.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create API client with convenience methods</name>
  <files>frontend/src/lib/api/client.ts, frontend/tests/api/client.test.ts</files>
  <behavior>
    - Test 1: apiClient makes GET request and returns typed response
    - Test 2: apiClient makes POST request with JSON body
    - Test 3: apiClient throws ApiError on non-ok response
    - Test 4: apiClient throws NetworkError on fetch failure
    - Test 5: api.get/post/put/delete convenience methods work
    - Test 6: Custom headers are merged with defaults
  </behavior>
  <action>
    Create centralized fetch wrapper following RESEARCH.md Pattern 2.

    1. Create `frontend/src/lib/api/client.ts`:
       - Define API_BASE constant from process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000'
       - Define RequestOptions interface extending Omit<RequestInit, 'body'> with optional body (unknown)
       - Create async `handleResponse<T>(response: Response): Promise<T>`:
         - Check response.ok, throw ApiError if not (try to parse JSON for data)
         - Return response.json() as Promise<T>
       - Export async `apiClient<T>(endpoint: string, options?: RequestOptions): Promise<T>`:
         - Destructure body, headers, ...restOptions from options
         - Build config with Content-Type: application/json header
         - Stringify body if provided
         - Wrap fetch in try/catch:
           - Re-throw ApiError as-is
           - Wrap other errors in NetworkError
       - Export `api` object with get/post/put/delete convenience methods

    2. Create `frontend/tests/api/client.test.ts`:
       - Use vi.stubGlobal('fetch', vi.fn()) for mocking
       - Test successful GET returns typed data
       - Test POST with body serializes correctly
       - Test HTTP errors throw ApiError with response data
       - Test network errors throw NetworkError
       - Test convenience methods call apiClient with correct method
       - Test custom headers merge with defaults

    Run `npm test -- --run` from frontend directory.
  </action>
  <verify>
    <automated>cd /home/guyi/structureclaw/frontend && npm test -- --run api/client</automated>
  </verify>
  <done>
    API client exports apiClient and api object.
    Tests pass verifying all error handling and convenience methods.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create barrel export</name>
  <files>frontend/src/lib/api/index.ts</files>
  <action>
    Create barrel export for API module.

    1. Create `frontend/src/lib/api/index.ts`:
       - Re-export ApiError, NetworkError from ./errors
       - Re-export apiClient, api from ./client
       - Re-export RequestOptions type from ./client (if needed externally)

    Run `npm test -- --run` and `npm run type-check` from frontend directory.
  </action>
  <verify>
    <automated>cd /home/guyi/structureclaw/frontend && npm test -- --run && npm run type-check</automated>
  </verify>
  <done>
    API module exports available via @/lib/api.
    All tests pass, type-check succeeds.
  </done>
</task>

</tasks>

<verification>
- [ ] ApiError includes status, statusText, and optional data
- [ ] NetworkError thrown on fetch failures (not HTTP errors)
- [ ] apiClient handles JSON serialization automatically
- [ ] Convenience methods (get, post, put, delete) work correctly
- [ ] All tests pass with `npm test -- --run`
</verification>

<success_criteria>
1. API client centralizes fetch logic with consistent error handling
2. Typed responses work with TypeScript generics
3. ApiError thrown for HTTP errors (4xx, 5xx)
4. NetworkError thrown for network failures
5. All tests pass with >90% coverage on new code
</success_criteria>

<output>
After completion, create `.planning/phases/04-state-api-layer/04-02-SUMMARY.md`
</output>
