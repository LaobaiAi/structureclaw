---
phase: 04-state-api-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/stores/context.tsx
  - frontend/src/lib/stores/slices/console.ts
  - frontend/src/lib/stores/index.ts
  - frontend/tests/stores/context.test.tsx
  - frontend/tests/stores/slices/console.test.ts
  - frontend/tests/setup.ts
autonomous: true
requirements: [STAT-01]
user_setup: []
must_haves:
  truths:
    - "Store creates a new instance per request via React Context"
    - "useStore hook throws clear error when used outside provider"
    - "Console slice manages endpoint, mode, conversationId, traceId state"
    - "Store persists correctly during SSR hydration"
  artifacts:
    - path: "frontend/src/lib/stores/context.tsx"
      provides: "Store factory with Context provider pattern"
      exports: ["AppStoreProvider", "useStore", "createAppStore"]
    - path: "frontend/src/lib/stores/slices/console.ts"
      provides: "Console state slice with actions"
      contains: "createConsoleSlice"
    - path: "frontend/tests/stores/context.test.tsx"
      provides: "Unit tests for store factory"
    - path: "frontend/tests/stores/slices/console.test.ts"
      provides: "Unit tests for console slice"
  key_links:
    - from: "frontend/src/app/providers.tsx"
      to: "frontend/src/lib/stores/context.tsx"
      via: "AppStoreProvider wrapper"
      pattern: "AppStoreProvider"
---

<objective>
Implement SSR-safe Zustand store using factory pattern with React Context.

Purpose: Prevent state leakage between requests in Next.js App Router by creating a new store instance per request using React Context provider pattern.
Output: Store factory, console slice, and comprehensive test coverage.
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-state-api-layer/04-RESEARCH.md
@.planning/phases/04-state-api-layer/04-VALIDATION.md

<interfaces>
<!-- Existing interfaces from codebase -->

From frontend/src/app/providers.tsx:
```typescript
export function Providers({ children }: { children: React.ReactNode })
```

From frontend/vitest.config.ts:
```typescript
// Test files: tests/**/*.test.ts, tests/**/*.test.tsx
// Setup file: tests/setup.ts
// Alias: @/ -> ./src/
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create console state slice</name>
  <files>frontend/src/lib/stores/slices/console.ts, frontend/tests/stores/slices/console.test.ts</files>
  <behavior>
    - Test 1: createConsoleSlice returns initial state with correct defaults
    - Test 2: setEndpoint action updates endpoint value
    - Test 3: setMode action updates mode value
    - Test 4: setConversationId action updates conversationId
    - Test 5: resetConsole action restores initial state
  </behavior>
  <action>
    Create the console slice following Zustand slice pattern from RESEARCH.md Pattern 1.

    1. Create `frontend/src/lib/stores/slices/console.ts`:
       - Define `ConsoleState` interface with: endpoint ('agent-run' | 'chat-message' | 'chat-execute'), mode ('chat' | 'execute' | 'auto'), conversationId (string | null), traceId (string | null)
       - Define `ConsoleActions` interface with: setEndpoint, setMode, setConversationId, resetConsole
       - Export `ConsoleSlice` type = ConsoleState & ConsoleActions
       - Export `initialConsoleState` constant with defaults: endpoint='chat-message', mode='auto', conversationId=null, traceId=null
       - Export `createConsoleSlice` function using StateCreator pattern

    2. Create `frontend/tests/stores/slices/console.test.ts`:
       - Test each action updates state correctly
       - Test resetConsole restores initial state
       - Follow existing test patterns from Phase 02-03

    Run `npm test -- --run` from frontend directory after implementation.
  </action>
  <verify>
    <automated>cd /home/guyi/structureclaw/frontend && npm test -- --run console</automated>
  </verify>
  <done>
    Console slice exports createConsoleSlice with all actions working.
    Tests pass verifying state mutations and reset functionality.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create store context and provider</name>
  <files>frontend/src/lib/stores/context.tsx, frontend/tests/stores/context.test.tsx</files>
  <behavior>
    - Test 1: AppStoreProvider renders children without error
    - Test 2: useStore throws error when used outside AppStoreProvider
    - Test 3: useStore returns selected state from store
    - Test 4: Multiple components share same store instance within provider
    - Test 5: Store state updates propagate to all consumers
  </behavior>
  <action>
    Create the store context and provider following RESEARCH.md Pattern 1 (Zustand docs + GitHub discussion #2326).

    1. Create `frontend/src/lib/stores/context.tsx`:
       - Use 'use client' directive
       - Import createContext, useContext, useRef from React
       - Import type StoreApi, useStore as useZustandStore from zustand
       - Import createStore from zustand/vanilla
       - Define `StoreState` type combining all slice types (start with ConsoleSlice)
       - Export `initStore()` function returning initial state
       - Export `createAppStore(initState?)` function using createStore pattern
       - Export `AppStoreContext` with StoreApi<StoreState> | null type
       - Export `AppStoreProvider` component using useRef to hold store instance
       - Export `useStore<T>(selector)` hook that throws if outside provider

    2. Create `frontend/tests/stores/context.test.tsx`:
       - Use @testing-library/react render and screen
       - Test provider renders children
       - Test useStore throws without provider (use console.error spy)
       - Test state selection and updates work correctly
       - Test multiple components share state

    Run `npm test -- --run` from frontend directory after implementation.
  </action>
  <verify>
    <automated>cd /home/guyi/structureclaw/frontend && npm test -- --run stores/context</automated>
  </verify>
  <done>
    Store context exports AppStoreProvider and useStore hook.
    Tests pass verifying SSR-safe factory pattern and context behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create barrel export and integrate with providers</name>
  <files>frontend/src/lib/stores/index.ts, frontend/src/app/providers.tsx</files>
  <action>
    Create barrel export and integrate store provider into existing provider tree.

    1. Create `frontend/src/lib/stores/index.ts`:
       - Re-export AppStoreProvider, useStore, createAppStore, initStore from ./context
       - Re-export ConsoleSlice types from ./slices/console

    2. Update `frontend/src/app/providers.tsx`:
       - Import AppStoreProvider from '@/lib/stores'
       - Wrap existing providers with AppStoreProvider (inside QueryClientProvider, outside ThemeProvider for now)
       - Final order: QueryClientProvider > AppStoreProvider > ThemeProvider > children

    Run `npm test -- --run` and `npm run type-check` from frontend directory.
  </action>
  <verify>
    <automated>cd /home/guyi/structureclaw/frontend && npm test -- --run && npm run type-check</automated>
  </verify>
  <done>
    Store exports available via @/lib/stores.
    AppStoreProvider integrated in provider tree.
    All tests pass, type-check succeeds.
  </done>
</task>

</tasks>

<verification>
- [ ] Console slice handles all required state fields (endpoint, mode, conversationId, traceId)
- [ ] Store factory creates new instance per request (verified by tests)
- [ ] useStore throws descriptive error outside provider
- [ ] Provider integrated in existing provider tree
- [ ] All tests pass with `npm test -- --run`
</verification>

<success_criteria>
1. Zustand stores use factory pattern with React Context for SSR compatibility
2. Console slice manages endpoint, mode, conversationId, traceId with reset capability
3. useStore hook throws clear error when used outside AppStoreProvider
4. All tests pass with >90% coverage on new code
</success_criteria>

<output>
After completion, create `.planning/phases/04-state-api-layer/04-01-SUMMARY.md`
</output>
