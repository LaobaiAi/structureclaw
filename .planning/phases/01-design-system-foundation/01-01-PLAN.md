---
phase: 01-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: [01-00]
files_modified:
  - frontend/src/app/globals.css
  - frontend/tailwind.config.js
autonomous: true
requirements: [DSGN-01, DSGN-03]
user_setup: []
must_haves:
  truths:
    - "All colors are defined as CSS custom properties (semantic tokens)"
    - "Light and dark theme variables exist with proper pairing"
    - "Tailwind config references CSS variables for all theme colors"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "CSS custom properties for design tokens"
      contains: ":root", ".dark"
      min_lines: 50
    - path: "frontend/tailwind.config.js"
      provides: "Tailwind theme extension with CSS variable references"
      contains: "darkMode: 'class'"
  key_links:
    - from: "tailwind.config.js"
      to: "globals.css"
      via: "hsl(var(--color-name)) references"
      pattern: "hsl\\(var\\(--"
---

<objective>
Replace existing hardcoded CSS variables with a complete semantic design token system using shadcn/ui's background/foreground pairing convention.

Purpose: Establish a flicker-free, theme-aware color system that works with next-themes.
Output: CSS variables for all semantic colors with light/dark variants, Tailwind config updated to reference them.
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md
@.planning/phases/01-design-system-foundation/01-VALIDATION.md
</context>

<interfaces>
<!-- Key types from existing codebase. Extracted for executor reference. -->

From frontend/tailwind.config.js:
```javascript
// Already has CSS variable references - extend this pattern
colors: {
  border: "hsl(var(--border))",
  background: "hsl(var(--background))",
  foreground: "hsl(var(--foreground))",
  primary: { DEFAULT: "hsl(var(--primary))", foreground: "hsl(var(--primary-foreground))" },
  // ... other colors
}
borderRadius: {
  lg: "var(--radius)",
  md: "calc(var(--radius) - 2px)",
  sm: "calc(var(--radius) - 4px)",
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace globals.css with semantic design tokens</name>
  <files>frontend/src/app/globals.css</files>
  <behavior>
    - Test 1: :root should contain --background, --foreground, --primary, --primary-foreground, --secondary, --secondary-foreground, --muted, --muted-foreground, --accent, --accent-foreground, --destructive, --destructive-foreground, --border, --input, --ring
    - Test 2: .dark selector should override all color variables with dark theme values
    - Test 3: --radius variable should be defined
    - Test 4: Font CSS variables (--font-sans, --font-mono) should be defined as placeholders (updated in plan 02)
  </behavior>
  <action>
    Replace the existing `globals.css` content (keeping Tailwind directives) with a complete design token system following shadcn/ui convention.

    Use HSL format for broader browser compatibility (research recommended hsl over oklch).

    Structure:
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    @layer base {
      :root {
        /* Background/Foreground pairs */
        --background: 0 0% 100%;
        --foreground: 222.2 84% 4.9%;

        /* Card */
        --card: 0 0% 100%;
        --card-foreground: 222.2 84% 4.9%;

        /* Popover */
        --popover: 0 0% 100%;
        --popover-foreground: 222.2 84% 4.9%;

        /* Primary - use StructureClaw's orange accent */
        --primary: 24 95% 53%; /* #ff7f11 equivalent in HSL */
        --primary-foreground: 0 0% 100%;

        /* Secondary */
        --secondary: 210 40% 96.1%;
        --secondary-foreground: 222.2 47.4% 11.2%;

        /* Muted */
        --muted: 210 40% 96.1%;
        --muted-foreground: 215.4 16.3% 46.9%;

        /* Accent - teal secondary accent */
        --accent: 172 66% 50%; /* #2ec4b6 equivalent */
        --accent-foreground: 222.2 47.4% 11.2%;

        /* Destructive */
        --destructive: 0 84.2% 60.2%;
        --destructive-foreground: 210 40% 98%;

        /* Border & Input */
        --border: 214.3 31.8% 91.4%;
        --input: 214.3 31.8% 91.4%;

        /* Ring */
        --ring: 24 95% 53%;

        /* Radius */
        --radius: 0.5rem;

        /* Font placeholders (set in plan 02) */
        --font-sans: ui-sans-serif, system-ui, sans-serif;
        --font-mono: ui-monospace, monospace;
      }

      .dark {
        --background: 222.2 84% 4.9%;
        --foreground: 210 40% 98%;

        --card: 222.2 84% 4.9%;
        --card-foreground: 210 40% 98%;

        --popover: 222.2 84% 4.9%;
        --popover-foreground: 210 40% 98%;

        --primary: 24 95% 53%;
        --primary-foreground: 0 0% 100%;

        --secondary: 217.2 32.6% 17.5%;
        --secondary-foreground: 210 40% 98%;

        --muted: 217.2 32.6% 17.5%;
        --muted-foreground: 215 20.2% 65.1%;

        --accent: 172 66% 50%;
        --accent-foreground: 210 40% 98%;

        --destructive: 0 62.8% 30.6%;
        --destructive-foreground: 210 40% 98%;

        --border: 217.2 32.6% 17.5%;
        --input: 217.2 32.6% 17.5%;

        --ring: 24 95% 53%;
      }
    }

    @layer base {
      * {
        @apply border-border;
      }
      body {
        @apply bg-background text-foreground;
        font-feature-settings: "rlig" 1, "calt" 1;
      }
    }
    ```

    IMPORTANT: Remove ALL old custom CSS classes (.console-root, .console-hero, etc.) - they will be replaced by Tailwind utilities in later phases.
  </action>
  <verify>
    <automated>cd frontend && npm run test:run tests/design-tokens.test.ts</automated>
  </verify>
  <done>CSS variables defined for all semantic colors with light/dark variants, old custom CSS removed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Tailwind config for design tokens</name>
  <files>frontend/tailwind.config.js</files>
  <behavior>
    - Test 1: config should have darkMode: 'class'
    - Test 2: config.theme.extend.colors should reference all CSS variables via hsl(var(--name))
    - Test 3: config.theme.extend.fontFamily should reference --font-sans and --font-mono
  </behavior>
  <action>
    Update `tailwind.config.js` to:
    1. Set `darkMode: 'class'` (required for next-themes)
    2. Add fontFamily configuration referencing CSS variables
    3. Ensure all color references use hsl() wrapper

    Update the config:
    ```javascript
    /** @type {import('tailwindcss').Config} */
    module.exports = {
      darkMode: 'class', // Required for next-themes
      content: [
        './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
        './src/components/**/*.{js,ts,jsx,tsx,mdx}',
        './src/app/**/*.{js,ts,jsx,tsx,mdx}',
      ],
      theme: {
        extend: {
          colors: {
            border: 'hsl(var(--border))',
            input: 'hsl(var(--input))',
            ring: 'hsl(var(--ring))',
            background: 'hsl(var(--background))',
            foreground: 'hsl(var(--foreground))',
            primary: {
              DEFAULT: 'hsl(var(--primary))',
              foreground: 'hsl(var(--primary-foreground))',
            },
            secondary: {
              DEFAULT: 'hsl(var(--secondary))',
              foreground: 'hsl(var(--secondary-foreground))',
            },
            destructive: {
              DEFAULT: 'hsl(var(--destructive))',
              foreground: 'hsl(var(--destructive-foreground))',
            },
            muted: {
              DEFAULT: 'hsl(var(--muted))',
              foreground: 'hsl(var(--muted-foreground))',
            },
            accent: {
              DEFAULT: 'hsl(var(--accent))',
              foreground: 'hsl(var(--accent-foreground))',
            },
            popover: {
              DEFAULT: 'hsl(var(--popover))',
              foreground: 'hsl(var(--popover-foreground))',
            },
            card: {
              DEFAULT: 'hsl(var(--card))',
              foreground: 'hsl(var(--card-foreground))',
            },
          },
          borderRadius: {
            lg: 'var(--radius)',
            md: 'calc(var(--radius) - 2px)',
            sm: 'calc(var(--radius) - 4px)',
          },
          fontFamily: {
            sans: ['var(--font-sans)'],
            mono: ['var(--font-mono)'],
          },
          keyframes: {
            'accordion-down': {
              from: { height: '0' },
              to: { height: 'var(--radix-accordion-content-height)' },
            },
            'accordion-up': {
              from: { height: 'var(--radix-accordion-content-height)' },
              to: { height: '0' },
            },
          },
          animation: {
            'accordion-down': 'accordion-down 0.2s ease-out',
            'accordion-up': 'accordion-up 0.2s ease-out',
          },
        },
      },
      plugins: [],
    }
    ```
  </action>
  <verify>
    <automated>cd frontend && npm run test:run tests/tailwind-config.test.ts</automated>
  </verify>
  <done>Tailwind config has darkMode: 'class', all color variables referenced, fontFamily configured</done>
</task>

<task type="auto">
  <name>Task 3: Update existing components to use new tokens</name>
  <files>
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/card.tsx
  </files>
  <action>
    Update Button variants to use semantic color tokens instead of hardcoded colors:

    In `button.tsx`, update variants:
    ```typescript
    variant: {
      default: "bg-primary text-primary-foreground hover:bg-primary/90",
      destructive: "bg-destructive text-destructive-foreground hover:bg-destructive/90",
      outline: "border border-input bg-background hover:bg-accent hover:text-accent-foreground",
      secondary: "bg-secondary text-secondary-foreground hover:bg-secondary/80",
      ghost: "hover:bg-accent hover:text-accent-foreground",
      link: "text-primary underline-offset-4 hover:underline",
    },
    ```

    Card component already uses CSS variables correctly - no changes needed.
  </action>
  <verify>
    <automated>cd frontend && npm run type-check</automated>
  </verify>
  <done>Button uses semantic color tokens, no hardcoded color values</done>
</task>

</tasks>

<verification>
1. Run `npm run test:run tests/design-tokens.test.ts tests/tailwind-config.test.ts` - tests should pass
2. Run `npm run type-check` - no type errors
3. Visual check: `npm run dev` and verify Tailwind classes work with new tokens
</verification>

<success_criteria>
- CSS variables defined for all semantic colors in :root and .dark
- Tailwind config has darkMode: 'class'
- All color utilities in Tailwind reference CSS variables
- Button component uses semantic tokens
- Type check passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-01-SUMMARY.md`
</output>
