---
phase: 01-design-system-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vitest.config.ts
  - frontend/tests/setup.ts
  - frontend/tests/design-tokens.test.ts
  - frontend/tests/fonts.test.ts
  - frontend/tests/tailwind-config.test.ts
  - frontend/tests/cn-utility.test.ts
  - frontend/tests/theme-switch.test.tsx
  - frontend/tests/accent-color.test.ts
  - frontend/tests/glassmorphism.test.ts
autonomous: true
requirements: [DSGN-01, DSGN-02, DSGN-03, DSGN-04, DSGN-05, DSGN-06, DSGN-07]
user_setup: []
must_haves:
  truths:
    - "Test framework runs and can execute tests"
    - "All test files exist with passing stubs"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "Vitest configuration for Next.js 14"
      exports: ["defineConfig"]
    - path: "frontend/tests/setup.ts"
      provides: "Test setup with testing-library"
    - path: "frontend/tests/*.test.ts(x)"
      provides: "Test stubs for each requirement"
  key_links:
    - from: "vitest.config.ts"
      to: "@testing-library/react"
      via: "setup configuration"
---

<objective>
Set up Vitest test infrastructure and create test stubs for all Phase 1 requirements.

Purpose: Establish automated verification capability before implementation begins.
Output: Working test framework with stubs that pass for future implementation.
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md
@.planning/phases/01-design-system-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and testing dependencies</name>
  <files>frontend/package.json</files>
  <action>
    In the frontend directory, install testing dependencies:

    ```bash
    cd frontend && npm install -D vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom jsdom @types/node
    ```

    This adds Vitest as the test framework with React Testing Library support. Do NOT install vitest as a production dependency.
  </action>
  <verify>
    <automated>cd frontend && npm ls vitest @testing-library/react --depth=0</automated>
  </verify>
  <done>Vitest and testing dependencies are installed as devDependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create Vitest configuration</name>
  <files>frontend/vitest.config.ts</files>
  <action>
    Create `vitest.config.ts` in the frontend directory with configuration for Next.js 14:

    ```typescript
    import { defineConfig } from 'vitest/config'
    import react from '@vitejs/plugin-react'
    import path from 'path'

    export default defineConfig({
      plugins: [react()],
      test: {
        environment: 'jsdom',
        setupFiles: ['./tests/setup.ts'],
        include: ['tests/**/*.test.{ts,tsx}'],
        globals: true,
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, './src'),
        },
      },
    })
    ```

    Add test script to package.json:
    ```json
    "test": "vitest",
    "test:run": "vitest run"
    ```
  </action>
  <verify>
    <automated>cd frontend && npm run test:run -- --passWithNoTests</automated>
  </verify>
  <done>Vitest config exists, test commands work (passes with no tests)</done>
</task>

<task type="auto">
  <name>Task 3: Create test setup file</name>
  <files>frontend/tests/setup.ts</files>
  <action>
    Create `tests/setup.ts` with Testing Library configuration:

    ```typescript
    import '@testing-library/jest-dom'
    ```

    This file imports jest-dom matchers for assertions like `toBeInTheDocument()`.
  </action>
  <verify>
    <automated>test -f frontend/tests/setup.ts</automated>
  </verify>
  <done>Test setup file exists and imports jest-dom matchers</done>
</task>

<task type="auto">
  <name>Task 4: Create test stubs for all requirements</name>
  <files>
    frontend/tests/design-tokens.test.ts
    frontend/tests/fonts.test.ts
    frontend/tests/tailwind-config.test.ts
    frontend/tests/cn-utility.test.ts
    frontend/tests/theme-switch.test.tsx
    frontend/tests/accent-color.test.ts
    frontend/tests/glassmorphism.test.ts
  </files>
  <action>
    Create test stub files that will be implemented during the phase. Each file should have a basic passing test structure.

    Create `tests/design-tokens.test.ts`:
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('Design Tokens (DSGN-01)', () => {
      it.todo('should define CSS variables for colors in :root')
      it.todo('should define CSS variables for colors in .dark')
      it.todo('should use background/foreground pairing convention')
    })
    ```

    Create `tests/fonts.test.ts`:
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('Geist Font (DSGN-02)', () => {
      it.todo('should configure Geist Sans font')
      it.todo('should configure Geist Mono font')
      it.todo('should apply font variables to html element')
    })
    ```

    Create `tests/tailwind-config.test.ts`:
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('Tailwind Configuration (DSGN-03)', () => {
      it.todo('should have darkMode set to class')
      it.todo('should reference CSS variables for colors')
      it.todo('should define fontFamily with CSS variable references')
    })
    ```

    Create `tests/cn-utility.test.ts`:
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { cn } from '@/lib/utils'

    describe('cn() Utility (DSGN-04)', () => {
      it('should merge class names', () => {
        expect(cn('foo', 'bar')).toBe('foo bar')
      })

      it('should handle conditional classes', () => {
        expect(cn('foo', false && 'bar', 'baz')).toBe('foo baz')
      })

      it('should merge conflicting Tailwind classes', () => {
        expect(cn('p-4', 'p-2')).toBe('p-2')
      })
    })
    ```

    Create `tests/theme-switch.test.tsx`:
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { render, screen } from '@testing-library/react'

    describe('Theme Switching (DSGN-05)', () => {
      it.todo('should render theme toggle component')
      it.todo('should switch between light, dark, and system themes')
      it.todo('should persist theme preference')
    })
    ```

    Create `tests/accent-color.test.ts`:
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('Custom Accent Color (DSGN-06)', () => {
      it.todo('should define accent color CSS variable')
      it.todo('should define accent-foreground color CSS variable')
      it.todo('should have light and dark variants')
    })
    ```

    Create `tests/glassmorphism.test.ts`:
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('Glassmorphism Effects (DSGN-07)', () => {
      it.todo('should provide backdrop-blur utility classes')
      it.todo('should define glass card component variants')
    })
    ```
  </action>
  <verify>
    <automated>cd frontend && npm run test:run</automated>
  </verify>
  <done>All test stub files exist and pass with todo tests</done>
</task>

</tasks>

<verification>
Run `npm run test:run` in frontend directory. All tests should pass (todo tests count as passing).
</verification>

<success_criteria>
- Vitest installed and configured
- `npm run test` and `npm run test:run` commands work
- All 7 test stub files exist
- `npm run test:run` passes (todo tests count as passing)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-00-SUMMARY.md`
</output>
