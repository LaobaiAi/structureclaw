---
phase: 01-design-system-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - frontend/package.json
  - frontend/src/components/theme-provider.tsx
  - frontend/src/components/theme-toggle.tsx
  - frontend/src/app/layout.tsx
  - frontend/src/app/providers.tsx
autonomous: true
requirements: [DSGN-05]
user_setup: []
must_haves:
  truths:
    - "User can switch between light, dark, and system theme preferences"
    - "Theme preference persists across sessions"
    - "No page flash when loading (flicker-free)"
    - "Theme syncs across browser tabs"
  artifacts:
    - path: "frontend/src/components/theme-provider.tsx"
      provides: "next-themes wrapper component"
      exports: ["ThemeProvider"]
    - path: "frontend/src/components/theme-toggle.tsx"
      provides: "Dark/Light/System toggle UI"
      exports: ["ThemeToggle"]
    - path: "frontend/src/app/providers.tsx"
      provides: "Combined providers including ThemeProvider"
  key_links:
    - from: "providers.tsx"
      to: "theme-provider.tsx"
      via: "import and wrap children"
    - from: "layout.tsx"
      to: "providers.tsx"
      via: "Providers component with suppressHydrationWarning"
---

<objective>
Implement tri-state theme switching (Light/Dark/System) using next-themes with flicker-free SSR support.

Purpose: Enable users to choose their preferred color scheme with persistence.
Output: ThemeProvider component, ThemeToggle UI component, integrated into root layout.
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md
@.planning/phases/01-design-system-foundation/01-VALIDATION.md
</context>

<interfaces>
From frontend/src/app/providers.tsx:
```typescript
export function Providers({ children }: { children: React.ReactNode })
// Currently wraps QueryClientProvider
// Will be extended to include ThemeProvider
```

From frontend/src/app/layout.tsx:
```typescript
// Already has suppressHydrationWarning on html element (from plan 02)
// html className includes font variables
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install next-themes package</name>
  <files>frontend/package.json</files>
  <action>
    Install next-themes for theme management:

    ```bash
    cd frontend && npm install next-themes
    ```

    next-themes provides SSR-safe theme switching with localStorage persistence and system preference detection.
  </action>
  <verify>
    <automated>cd frontend && npm ls next-themes --depth=0</automated>
  </verify>
  <done>next-themes package installed</done>
</task>

<task type="auto">
  <name>Task 2: Create ThemeProvider component</name>
  <files>frontend/src/components/theme-provider.tsx</files>
  <action>
    Create `src/components/theme-provider.tsx`:

    ```typescript
    'use client'

    import * as React from 'react'
    import { ThemeProvider as NextThemesProvider } from 'next-themes'
    import { type ThemeProviderProps } from 'next-themes'

    export function ThemeProvider({ children, ...props }: ThemeProviderProps) {
      return <NextThemesProvider {...props}>{children}</NextThemesProvider>
    }
    ```

    This wrapper allows us to:
    - Use 'use client' directive for client-side only
    - Customize props defaults in one place
    - Add any future theme-related logic
  </action>
  <verify>
    <automated>cd frontend && npm run type-check</automated>
  </verify>
  <done>ThemeProvider component created with next-themes wrapper</done>
</task>

<task type="auto">
  <name>Task 3: Create ThemeToggle component</name>
  <files>frontend/src/components/theme-toggle.tsx</files>
  <action>
    Create `src/components/theme-toggle.tsx` with tri-state dropdown:

    ```typescript
    'use client'

    import * as React from 'react'
    import { Moon, Sun, Monitor } from 'lucide-react'
    import { useTheme } from 'next-themes'

    import { Button } from '@/components/ui/button'
    import {
      DropdownMenu,
      DropdownMenuContent,
      DropdownMenuItem,
      DropdownMenuTrigger,
    } from '@/components/ui/dropdown-menu'
    import { cn } from '@/lib/utils'

    export function ThemeToggle({ className }: { className?: string }) {
      const { setTheme, theme, resolvedTheme } = useTheme()
      const [mounted, setMounted] = React.useState(false)

      // Prevent hydration mismatch
      React.useEffect(() => setMounted(true), [])

      if (!mounted) {
        return (
          <Button variant="ghost" size="icon" className={cn('h-9 w-9', className)} disabled>
            <Sun className="h-5 w-5" />
            <span className="sr-only">Toggle theme</span>
          </Button>
        )
      }

      return (
        <DropdownMenu>
          <DropdownMenuTrigger asChild>
            <Button variant="ghost" size="icon" className={cn('h-9 w-9', className)}>
              <Sun className="h-5 w-5 rotate-0 scale-100 transition-all dark:-rotate-90 dark:scale-0" />
              <Moon className="absolute h-5 w-5 rotate-90 scale-0 transition-all dark:rotate-0 dark:scale-100" />
              <span className="sr-only">Toggle theme</span>
            </Button>
          </DropdownMenuTrigger>
          <DropdownMenuContent align="end">
            <DropdownMenuItem onClick={() => setTheme('light')}>
              <Sun className="mr-2 h-4 w-4" />
              Light
              {theme === 'light' && <span className="ml-auto text-xs">Active</span>}
            </DropdownMenuItem>
            <DropdownMenuItem onClick={() => setTheme('dark')}>
              <Moon className="mr-2 h-4 w-4" />
              Dark
              {theme === 'dark' && <span className="ml-auto text-xs">Active</span>}
            </DropdownMenuItem>
            <DropdownMenuItem onClick={() => setTheme('system')}>
              <Monitor className="mr-2 h-4 w-4" />
              System
              {theme === 'system' && <span className="ml-auto text-xs">Active</span>}
            </DropdownMenuItem>
          </DropdownMenuContent>
        </DropdownMenu>
      )
    }
    ```

    NOTE: This component requires DropdownMenu from shadcn/ui. Since we don't have it yet (Phase 2), we'll create a simplified version:

    ```typescript
    'use client'

    import * as React from 'react'
    import { Moon, Sun, Monitor } from 'lucide-react'
    import { useTheme } from 'next-themes'
    import { cn } from '@/lib/utils'

    export function ThemeToggle({ className }: { className?: string }) {
      const { setTheme, theme } = useTheme()
      const [mounted, setMounted] = React.useState(false)

      React.useEffect(() => setMounted(true), [])

      // Cycle through themes on click
      const cycleTheme = () => {
        const themes = ['light', 'dark', 'system'] as const
        const currentIndex = themes.indexOf(theme as typeof themes[number])
        const nextIndex = (currentIndex + 1) % themes.length
        setTheme(themes[nextIndex])
      }

      if (!mounted) {
        return (
          <button
            className={cn(
              'inline-flex h-9 w-9 items-center justify-center rounded-md text-sm font-medium',
              'transition-colors hover:bg-accent hover:text-accent-foreground',
              className
            )}
            disabled
          >
            <Sun className="h-5 w-5" />
            <span className="sr-only">Toggle theme</span>
          </button>
        )
      }

      return (
        <button
          onClick={cycleTheme}
          className={cn(
            'inline-flex h-9 w-9 items-center justify-center rounded-md text-sm font-medium',
            'transition-colors hover:bg-accent hover:text-accent-foreground',
            className
          )}
          title={`Current: ${theme}`}
        >
          <Sun className="h-5 w-5 rotate-0 scale-100 transition-all dark:-rotate-90 dark:scale-0" />
          <Moon className="absolute h-5 w-5 rotate-90 scale-0 transition-all dark:rotate-0 dark:scale-100" />
          <span className="sr-only">Toggle theme (current: {theme})</span>
        </button>
      )
    }
    ```

    This simplified version cycles through themes on click. We'll enhance with dropdown menu in Phase 2 when we build the component library.
  </action>
  <verify>
    <automated>cd frontend && npm run type-check</automated>
  </verify>
  <done>ThemeToggle component created with light/dark/system cycling</done>
</task>

<task type="auto">
  <name>Task 4: Integrate ThemeProvider into root layout</name>
  <files>
    frontend/src/app/providers.tsx
    frontend/src/app/layout.tsx
  </files>
  <action>
    Update `src/app/providers.tsx` to include ThemeProvider:

    ```typescript
    'use client'

    import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
    import { ThemeProvider } from '@/components/theme-provider'
    import { useState } from 'react'

    export function Providers({ children }: { children: React.ReactNode }) {
      const [queryClient] = useState(
        () =>
          new QueryClient({
            defaultOptions: {
              queries: {
                staleTime: 60 * 1000,
                refetchOnWindowFocus: false,
              },
            },
          })
      )

      return (
        <QueryClientProvider client={queryClient}>
          <ThemeProvider
            attribute="class"
            defaultTheme="system"
            enableSystem
            disableTransitionOnChange
          >
            {children}
          </ThemeProvider>
        </QueryClientProvider>
      )
    }
    ```

    Key ThemeProvider props:
    - `attribute="class"`: Use class-based dark mode (matches Tailwind darkMode: 'class')
    - `defaultTheme="system"`: Follow OS preference by default
    - `enableSystem`: Enable system preference detection
    - `disableTransitionOnChange`: Prevent flash during theme change

    The layout.tsx already has `suppressHydrationWarning` from plan 02 - no changes needed there.
  </action>
  <verify>
    <automated>cd frontend && npm run type-check</automated>
  </verify>
  <done>ThemeProvider integrated into providers, wraps children with theme context</done>
</task>

</tasks>

<verification>
1. Run `npm run type-check` - no type errors
2. Run `npm run build` - should build successfully
3. Visual check: `npm run dev` and verify:
   - Theme toggle appears
   - Clicking cycles through light/dark/system
   - Dark mode applies correctly
   - No flash on page reload
   - Theme persists on refresh
</verification>

<success_criteria>
- next-themes installed
- ThemeProvider component created
- ThemeToggle component created (simplified cycling version)
- ThemeProvider integrated in providers.tsx
- suppressHydrationWarning on html element (already done)
- Type check passes
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-04-SUMMARY.md`
</output>
