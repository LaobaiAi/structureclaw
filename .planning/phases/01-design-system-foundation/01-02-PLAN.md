---
phase: 01-design-system-foundation
plan: 02
type: execute
wave: 1
depends_on: [01-00]
files_modified:
  - frontend/package.json
  - frontend/src/lib/fonts.ts
  - frontend/src/app/layout.tsx
  - frontend/src/app/globals.css
autonomous: true
requirements: [DSGN-02]
user_setup: []
must_haves:
  truths:
    - "Geist Sans font renders correctly across the application"
    - "Geist Mono font renders correctly for code elements"
    - "Font CSS variables are set in the html element"
  artifacts:
    - path: "frontend/src/lib/fonts.ts"
      provides: "Geist font configuration"
      exports: ["GeistSans", "GeistMono"]
    - path: "frontend/src/app/layout.tsx"
      provides: "Font application to html element"
      contains: "GeistSans.variable"
  key_links:
    - from: "layout.tsx"
      to: "fonts.ts"
      via: "import and className application"
      pattern: "className.*variable"
---

<objective>
Configure Geist font family (Sans + Mono) using the official npm package with Next.js font optimization.

Purpose: Establish modern typography matching the Linear/Vercel aesthetic.
Output: Geist fonts loaded via next/font, CSS variables set for Tailwind usage.
</objective>

<execution_context>
@/home/guyi/.claude/get-shit-done/workflows/execute-plan.md
@/home/guyi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md
@.planning/phases/01-design-system-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Geist font package</name>
  <files>frontend/package.json</files>
  <action>
    Install the official Geist font package:

    ```bash
    cd frontend && npm install geist
    ```

    This provides GeistSans and GeistMono exports optimized for Next.js.
  </action>
  <verify>
    <automated>cd frontend && npm ls geist --depth=0</automated>
  </verify>
  <done>geist package installed as dependency</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create fonts configuration file</name>
  <files>frontend/src/lib/fonts.ts</files>
  <behavior>
    - Test 1: should export GeistSans with variable property
    - Test 2: should export GeistMono with variable property
    - Test 3: font exports should be configured for Next.js optimization
  </behavior>
  <action>
    Create `src/lib/fonts.ts` with Geist font configuration:

    ```typescript
    import { GeistSans } from 'geist/font/sans'
    import { GeistMono } from 'geist/font/mono'

    export { GeistSans, GeistMono }
    ```

    The `geist` package exports pre-configured font objects with Next.js optimization built-in. Each export has a `.variable` property containing the CSS variable name (e.g., `--font-geist-sans`).
  </action>
  <verify>
    <automated>cd frontend && npm run test:run tests/fonts.test.ts</automated>
  </verify>
  <done>fonts.ts exports GeistSans and GeistMono with variable properties</done>
</task>

<task type="auto">
  <name>Task 3: Apply fonts to root layout</name>
  <files>
    frontend/src/app/layout.tsx
    frontend/src/app/globals.css
  </files>
  <action>
    Update `src/app/layout.tsx` to apply font variables to the html element:

    ```typescript
    import type { Metadata } from 'next'
    import './globals.css'
    import { Providers } from './providers'
    { GeistSans, GeistMono } from '@/lib/fonts'

    export const metadata: Metadata = {
      title: 'StructureClaw - 结构工程 AI 控制台',
      description: 'StructureClaw 前端控制台：统一调试 Agent 编排、Chat 路由与结构分析能力',
      keywords: ['结构分析', '有限元', '结构设计', 'Agent', 'Chat', 'OpenSees'],
    }

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      return (
        <html lang="zh-CN" className={`${GeistSans.variable} ${GeistMono.variable}`} suppressHydrationWarning>
          <body>
            <Providers>
              {children}
            </Providers>
          </body>
        </html>
      )
    }
    ```

    Key changes:
    - Import GeistSans and GeistMono from fonts.ts
    - Apply `.variable` classes to html element (sets --font-geist-sans, --font-geist-mono CSS variables)
    - Add `suppressHydrationWarning` (required for next-themes compatibility)

    Then update `globals.css` to set the font CSS variables:

    In the `:root` block, update font variables:
    ```css
    --font-sans: var(--font-geist-sans), ui-sans-serif, system-ui, sans-serif;
    --font-mono: var(--font-geist-mono), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    ```

    Also add to `.dark` block (same values):
    ```css
    --font-sans: var(--font-geist-sans), ui-sans-serif, system-ui, sans-serif;
    --font-mono: var(--font-geist-mono), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    ```
  </action>
  <verify>
    <automated>cd frontend && npm run type-check</automated>
  </verify>
  <done>Layout applies font variables, globals.css references them, suppressHydrationWarning set</done>
</task>

</tasks>

<verification>
1. Run `npm run test:run tests/fonts.test.ts` - tests should pass
2. Run `npm run type-check` - no type errors
3. Run `npm run build` - should build without font-related errors
4. Visual check: `npm run dev` and verify Geist font renders in browser DevTools
</verification>

<success_criteria>
- geist package installed
- fonts.ts exports GeistSans and GeistMono
- layout.tsx applies font variables to html element
- globals.css sets --font-sans and --font-mono to reference Geist variables
- suppressHydrationWarning added to html element
- Type check passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-02-SUMMARY.md`
</output>
